import java.io.*;

public class Recetario {
    private static File 
        directorio = new File("Recetario/");
    
    public boolean registrarReceta(Receta r) {
        boolean res = (r!=null);
        return res;
    }
}